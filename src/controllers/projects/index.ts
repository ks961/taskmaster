import { Request, Response } from "express";


export class ProjectsController {

    static async create(
        req: Request,
        res: Response
    ) {
        
    }

    static async projects(
        req: Request,
        res: Response
    ) {
        
    }

    static async invite(
        req: Request,
        res: Response
    ) {
        
    }
}
