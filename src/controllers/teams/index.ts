import { Request, Response } from "express";


export class TeamsController {

    static async create(
        req: Request,
        res: Response
    ) {
        
    }

    static async members(
        req: Request,
        res: Response
    ) {

    }

    static async invite(
        req: Request,
        res: Response
    ) {

    }
}
